<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pradeep.allinone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".audio.AudioAdapter" />
        <activity android:name=".audio.AudioList"/>
        <activity android:name=".video.VideoAdapter" />
        <activity android:name=".video.VideoActivity" />
        <activity android:name=".video.VideoList" />
        <activity android:name=".doc_text.DocTextActivity" />
        <activity android:name=".doc_text.DocTextAdapter" />
        <activity android:name=".pdf.PdfAdapter" />
        <activity android:name=".pdf.PdfActivity" />
        <activity android:name=".doc_text.DocTextList" />
        <activity android:name=".pdf.PdfList" />
        <activity android:name=".pacakagestart.StartActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>